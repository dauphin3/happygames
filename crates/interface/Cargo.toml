[package]
name = "interface"
version = "0.1.0"
edition = "2021"

[lib]
name = "interface"
crate-type = [ "cdylib", "rlib" ]

[dependencies]
hdk = "0.0.120"
serde = "1"
profile = { path = "../../zomes/profile"}